node_modules
.next
out
next-env.d.ts
*.tsbuildinfo
.DS_Store

# Workflow screenshot sources + client documents (converted copies live in
# public/works/ as WebP). Keep the originals local.
assets/GlowTheory Workflow Screenshots/
assets/IronPulse Workflow Screensots/

# Vapi config backups (contain credentials)
.vapi-backup/

# Secrets. .env.local holds the GoHighLevel PIT and location id; it is written
# by scripts/set-ghl-env.mjs and must never be committed. The .example file is
# the committed template and carries no values.
.env
.env.local
.env.*.local

# Impeccable working files: review captures are evidence, not assets
.impeccable/review/
